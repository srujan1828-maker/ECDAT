# Docker Compose configuration. Copy to .env.
# Set a strong shared token before exposing either service beyond localhost.
ECDAT_API_TOKEN=
# Optional explicitly authorized private target networks.
ECDAT_ALLOWED_CIDRS=
CORS_ORIGINS=http://localhost:3000
